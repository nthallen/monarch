<package> alicat
<include> "alicat_int.h"
<include> msg oui

<opts> "p:"
<sort>
  -p <port> Specify RS485 port

<switch>
  case 'p':
    Alicat_port = optarg; break;

<defs>
  DAS_IO::AppID_t DAS_IO::AppID("Alicat", "Alicat Mass Flow Controller Driver", "V1.0");
